Proceso laVida
	//Mientras (while) => se utiliza cuando no se cuantas iteraciones va a realizar el programa 
	//Para (for) => se utiliza cuando se sabe cuantas iteraciones vamos a realizar
	//Repetir (do...while) => se utiliza cuando es necesario realizar al menos una iteración y la condición se evalua al final
	//de la iteración
	Definir i Como Entero;
	Leer i;
	Mientras i <> 42 Hacer
		Escribir i;
		Leer i;
	FinMientras
FinProceso
